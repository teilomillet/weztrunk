#!/bin/sh

set -eu

dispatcher=${WEZTRUNK_AGENT_DISPATCHER:-"$HOME/.local/bin/weztrunk-agent"}

if [ ! -x "$dispatcher" ]; then
  printf 'WezTrunk agent dispatcher not found: %s\n' "$dispatcher" >&2
  exit 127
fi

exec "$dispatcher" commit
